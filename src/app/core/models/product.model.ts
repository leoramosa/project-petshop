export interface Product {
  id: number;
  nombre: string;
  price: number;
  description: string;
  fotoportada: string;
  quantity: number;
}

export interface Color {
  id: number;
  nomcolor: string;
}
