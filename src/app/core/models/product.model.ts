export interface Product {
  id: number;
  nombre: string;
  price: number;
  description: string;
  foto_portada: string;
  quantity: number;
}

export interface Color {
  id: number;
  nomcolor: string;
}
